global:
  scrape_interval: 50s #scraping interval duration
  scrape_timeout: 1s #waiting duration between each scrape

scrape_configs:
  - job_name: 'services'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['prometheus:9000', 'iris-prediction-api:5000']